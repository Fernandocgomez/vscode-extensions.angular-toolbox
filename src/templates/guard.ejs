import { inject } from '@angular/core';
import { CanActivateFn, Router, UrlTree } from '@angular/router';

export const <%= fnName %>: CanActivateFn = (route, state) => {
  const router = inject(Router);
  
  return true;
};