import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: '<%= selector %>',
  standalone: true,
  imports: [],
  <% if (inlineTemplate) { %>template: '',<% } else { %>templateUrl: './<%= componentNameAsKebabCase %>.component.html',<% } %>
  <% if (inlineStyle) { %>styles: [],<% } else { %>styleUrls: ['./<%= componentNameAsKebabCase %>.component.<%= stylesheetsFormat %>'],<% } %>
  changeDetection: <%= withOnPushChangeDetection ? 'ChangeDetectionStrategy.OnPush' : 'ChangeDetectionStrategy.Default' %>
})
export class <%= className %> {}